export type Blog = {
    // added_at?: number;
    author: string;
    title: string;
    description: string;
  };

export type fetchedBlog = {
    added_at: string;
    author: string;
    title: string;
    description: string;
  };